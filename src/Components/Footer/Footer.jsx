import React from "react";
import s from './Footer.module.css'

const Footer = (props) => {
	return (
		<div className={s.footer + ' ' + s.container}>
			<h1>Footer</h1>
		</div>
	)
}

export default Footer;